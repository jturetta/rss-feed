services:
  - type: web
    name: rss-feed-generator
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATA_DIR
        value: /app/data
      - key: PYTHONUNBUFFERED
        value: "1"
